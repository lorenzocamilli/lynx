query GetSenderRequests($limit: Int, $offset: Int) {
  senderRequests(limit: $limit, offset: $offset) {
    id
    url
    method
    response {
      id
      statusCode
      statusReason
    }
  }
}
